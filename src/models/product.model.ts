export interface SingleProduct {
	productId: number,
    name: string,
    description: string,
    price: number,
    priceWas: number,
    available: string,
    quantity: number,
    lowOnStock: string,
    promotionBadge: string,
    imageUrl: string
}
